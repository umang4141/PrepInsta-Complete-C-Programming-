#include<stdio.h>
#include<string.h>
extern int i=0;
int First_Ele(int n,int arr[10])
{
     int max=arr[0];
     int temp=0;
     for(i=n-1;i>=0;i--)
     {

         printf("%d ",arr[i]);
     }

}
int main()
{

    int arr[10];
    int n;
    printf("Enter the Number of Element;\n");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
    scanf("%d",&arr[i]);
    }
    First_Ele(n,arr);
}

